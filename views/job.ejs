
<!-- When adding- res.render("job", { job: null }) -->
 <!-- editing, you’ll do res.render("job", { job }) -->

 <!-- If job is null, then action="/job". But if job is not null, then action="/job/update/<%= job.id %> so that the update route is called. -->